[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.121.0",
    "pydantic>=2.12.3",
    "uvicorn[standard]>=0.38.0",
    "random-word",
]
